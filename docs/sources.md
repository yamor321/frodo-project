# מקורות מאומתים

כל הפרטים בקובץ זה נבדקו בפועל (ביקור בדפים חיים, לא רק חיפוש) ב-26.08.2026.
לגרסה המעוצבת המלאה של תכנית הארכיטקטורה (כולל אופציות מחסנית ומבנה תיקיות) ראו
את הארטיפקט שפורסם לפרויקט: "שקיפות מחירים כפר סבא".

## דף הקישורים המרכזי

https://www.gov.il/he/pages/cpfta_prices_regulations — רשות ההגנת הצרכן ולסחר
הוגן, כ-29 רשתות עם קישור "לצפייה במחירים" לכל אחת.

## הבסיס החוקי

- חוק קידום התחרות בענפי המזון והפארם, התשע"ד-2014 (חוק אב)
- תקנות קידום התחרות בענף המזון (שקיפות מחירים), התשע"ה-2014 (התקנות המפעילות)
  - סעיף 3(2): עדכון תוך שעה ממועד השינוי בקופה
  - סעיף 5: שמירת קבצים 3 חודשים ממועד הפרסום
- סף מחזור 250 מיליון ₪ מדווח בבריף הפרויקט, לא אומת ישירות בטקסט התקנה.

## כלי קוד פתוח קיים

github.com/OpenIsraeliSupermarkets/israeli-supermarket-scarpers (גם `il-supermarket-scraper` ב-PyPI) —
מכסה 30+ רשתות, תמיכה ב-binaprojects.com ו-publishedprices.co.il, בדיקות יומיות אוטומטיות.
שימש כנקודת התייחסות; הפרסר בפועל בפרויקט זה (`etl/scrapers/shufersal.py`) נכתב עצמאית מול
פורטל שופרסל (שאינו רץ על אחת משתי הפלטפורמות המשותפות, ולכן ממילא לא מכוסה ישירות שם).

## רשתות עם סניף מאומת בכפר סבא

| רשת | סניפים בכפר סבא (portal store id) | פלטפורמה | login | אמינות ידועה |
|---|---|---|---|---|
| שופרסל | 144, 394, 615, 682, 752, 845 | prices.shufersal.co.il | לא | לא דווחו תקלות; **נבחרה כרשת הפיילוט** |
| קרפור | 010, 400, 404, 471, 473 | prices.carrefour.co.il | לא | חי, ראו סעיף קרפור למטה |
| שוק העיר | 011 ("כפר סבא מזרח", אלי הורביץ 26) | Bina: shuk-hayir.binaprojects.com | לא | **חי, נבנה 28.08.2026** — ראו סעיף Bina למטה |
| רמי לוי | גלגלי הפלדה 5 + 2 סניפי "בשכונה" (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | חשד לא-מאומת לדיווח חלקי |
| יוחננוף | אתיר ידע 1 (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | ממצא ריקנות קבצים, לא מתוארך |
| אושר עד | דרך הים 9 (+ סניף מתוכנן) (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | לא נבדק |
| ויקטורי | אנגל 78, מרכז דמרי | laibcatalog.co.il/victory | לא | מקור השתנה בעבר, נדרש fallback; חסום מ-GitHub Actions |
| מחסני השוק | לוי אשכול 37 | laibcatalog.co.il/mshuk | לא | מקור השתנה בעבר, נדרש fallback |
| טיב טעם | נעמי שמיר 15, מתחם LEVINTECH (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | נמצא 28.08.2026, ב-CHAINS, לא נבנה עדיין |
| דור אלון (AM:PM) | צ'רניחובסקי 24 (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | נמצא 28.08.2026, ב-CHAINS, לא נבנה עדיין |
| Yellow | התע"ש 32 (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא*, סיסמה לא-ריקה | נמצא 28.08.2026, ב-CHAINS, לא נבנה עדיין |
| Stop Market | מתחם איינשטיין (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | נמצא 28.08.2026 (סבב שני), ב-CHAINS |
| Fresh Market | צ'רניחובסקי 16, מתחם G (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | נמצא 28.08.2026 (סבב שני), ב-CHAINS |
| קשת טעמים | נתיב האבות 14, מרכז שרונה (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא* | נמצא 28.08.2026 (סבב שני), ב-CHAINS |
| סאלח דבאח (מותג "Angus") | G Mall, ויצמן 207 (מס' סניף בפורטל: לא ידוע) | FTP: url.retail.publishedprices.co.il | לא*, סיסמה לא-ריקה | נמצא 28.08.2026 (סבב שני), ב-CHAINS |
| City Market | ויצמן 55, סניף 066 | citymarket-shops.co.il — פורטל עצמאי, לא login | לא | **נבדק חי 28.08.2026 ונפסל — המקור עצמו שבור** (ראו סעיף למטה), לא נבנה |
| Good Pharm (בית מרקחת) | ויצמן 113, ויצמן 88 | goodpharm.binaprojects.com (Bina) | לא | נמצא 28.08.2026 — **החלטת סקופ פתוחה: רשת פארם, לא סופר; רלוונטיות למחירי חלב מוטלת בספק** |
| Super-Pharm (בית מרקחת) | קניון ערים, "כפר סבא הירוקה" | prices.super-pharm.co.il — פורטל עצמאי, לא נבדק login | ? | נמצא 28.08.2026 — אותה החלטת סקופ פתוחה כמו Good Pharm |
| Wolt Market (dark store) | סניף וולט מרקט כפר סבא | wm-gateway.wolt.com — פלטפורמה חדשה, לא נבדקה | לא | נמצא 28.08.2026 — **לא סניף פיזי, dark store למשלוחים בלבד; לא מתאים למתודולוגיית ניווט/פינים על מפה כפי שהיא בנויה היום** |
| קינג סטור | פתיחה דווחה בעיתונות מקומית (כתובת מדויקת: לא ידועה) | Bina: binaprojects.com | לא | נמצא 28.08.2026, לא נבנה עדיין |
| שוק העיר | אלי הורביץ (מס' סניף בפורטל: לא ידוע) | Bina: binaprojects.com | לא | נמצא 28.08.2026, לא נבנה עדיין |
| חצי חינם | לא אותר סניף בעיר עצמה | shop.hazi-hinam.co.il | — | לא רלוונטי לפיילוט |

\* לא login אישי — FTP עם username קבוע לכל רשת (רוב הרשתות עם סיסמה ריקה, שלוש עם סיסמה אמיתית מפורסמת), מאומת חי. פירוט מלא בסעיף "publishedprices.co.il" למטה.

## שופרסל — מנגנון בפועל (מאומת מול הפורטל החי, 26.08.2026)

- קבצים מתארחים ב-Azure Blob (`pricesprodpublic.blob.core.windows.net`) עם SAS
  token חתום בקישור ההורדה עצמו בטבלת ה-HTML של הפורטל. ה-token פג תוקף באותו יום —
  יש להוריד מיד אחרי הליסטינג, לא לשמור קישורים לשימוש מאוחר.
- דפדוף: `?page=N`. מיון דטרמיניסטי לפי סניף: `?sort=Branch&sortdir=ASC`.
  **לא נמצא פרמטר סינון לפי סניף/קטגוריה** (נבדקו ולא עבדו: `?code=`, `?store=`,
  `?storeId=`, `?category=`, `?fileType=`, `?type=`) — הפתרון שיושם הוא מעבר על כל
  העמודים וסינון בצד הלקוח.
- קטגוריות זמינות (עמודת "קטגוריה" בטבלה): `price` (דלתא), `pricefull` (קטלוג
  מלא), `promo`, `promofull`, `stores`.
- סכימת XML מאומתת מקובץ אמיתי (`Root > ChainID/SubChainID/StoreID/BikoretNo/Items > Item > ...`):
  `ItemCode, ItemName, ManufactureName, ManufactureCountry, UnitQty, Quantity,
  UnitOfMeasure, bIsWeighted, QtyInPackage, ItemPrice, UnitOfMeasurePrice,
  AllowDiscount, PriceUpdateTime`.
- **אומת חי ב-26.08.2026**: כל 6 סניפי כפר סבא פרסמו PriceFull באותו יום
  (03:00–03:40), בין 147KB ל-430KB דחוס, 8,717 פריטים בקטלוג המלא של סניף 144
  לבדו.
- **סיכון מאומת בפועל, לא רק תיאורטי**: התאמת מוצרי חלב לפי מילת מפתח בשם המוצר
  מייצרת false positives אמיתיים — "שוקולד חלב" (milk chocolate) ו"גלידת שמנת"
  (cream-flavored ice cream) שניהם עולים בחיפוש מילות מפתח (חלב/שמנת) בלי להיות
  מוצרי החלב המפוקחים בפועל. ראה `tests/test_shufersal_parser.py::test_naive_dairy_keyword_match_has_false_positives`
  ו-`scripts/demo_shufersal.py`. זה בדיוק התרחיש שסעיף 4 בבריף מתכנן עבורו — סיווג
  מבוסס LLM מול רשימת קטגוריות סגורה, לא חיפוש מילת מפתח נאיבי.

## קובץ Stores — מיפוי סניף→עיר אובייקטיבי (אומת חי, 27.08.2026)

`https://prices.shufersal.co.il/` ממוין `?sort=Category&sortdir=DESC` מעלה קובץ
יחיד לכל הרשת (category=`stores`, store="All"). סכימה: `<Chain><ChainID/>
<ChainName/><SubChains><SubChain><SubChainID/><Stores><Store><StoreID/>
<BikoretNo/><StoreType/><StoreName/><Address/><City/><ZIPCode/></Store>...`

**`<City>` הוא קוד יישוב מספרי (סמל יישוב), לא שם עיר.** נבדק אמפירית: כל 6
הסניפים שזוהו ידנית (144, 394, 615, 682, 752, 845) חוזרים עם `City=6900` —
קוד היישוב הרשמי של כפר סבא. `etl/scrapers/shufersal.py:kfar_saba_stores()`
מסנן לפי הקוד הזה בפועל, לא לפי רשימה ידנית (`KFAR_SABA_STORE_IDS` נשארת רק
כפולבאק מתועד).

## בנצ'מרקים (שכבה 3)

| מקור | כתובת | פורמט | תדירות / היסטוריה |
|---|---|---|---|
| מחירים מפוקחים — data.gov.il (CKAN) | `data.gov.il/api/3/action/datastore_search?resource_id=0a760550-0426-4eb7-acf6-2ee919bf12e7` | **REST API, JSON נקי** | 391 רשומות = היסטוריה מלאה |
| מדד מחירים לצרכן — למ"ס | api.cbs.gov.il/index/data/price | API — XML/JSON/CSV/XLS | חודשי (15 לחודש), מ-1997 |
| מדד מחירי מזון עולמי — FAO | fao.org/worldfoodsituation/foodpricesindex | Excel + CSV | חודשי, מ-1990, 5 תתי-מדדים |

**הדף `gov.il/.../food-price-control-search` (HTML) נפסל** — חסום 403 ע"י
Cloudflare (`cdn-cgi/challenge-platform`), כולל ה-API הפנימי שלו
(`POST /he/api/DynamicCollector`, גם הוא 403 בבקשה ישירה, לא רק דרך דפדפן).
הוחלף ב-API של data.gov.il לעיל: `requests.get` רגיל, HTTP 200, ללא Cloudflare,
ללא צורך בדפדפן. שדות: `product, update date, retailer pricre without VAT
[sic — טעות כתיב במקור], consumers price includes VAT, consumer price in
Eilat`. **אין שדה קטגוריה/אריזה בפורמט הזה** — ראו `etl/category_mapping/
moag_matcher.py` לגבי איך זה נפתר (מפרט אריזה מגובש ידנית לכל מוצר, מאומת מול
עמוד ה-HTML בביקור נפרד לפני שנפסל כמקור). 11 מוצרי חלב רלוונטיים מתוך 22
המוצרים המפוקחים הכוללים (הרשימה כוללת גם חמאה, ביצים, לחם, מלח — לא רלוונטיים
לפיילוט חלב).

## עדכון היקף: הפילטר האובייקטיבי מצא יותר סניפים מהרשימה הידנית

הרצה חיה ב-28.08.2026 עם `kfar_saba_stores()` (סינון לפי `City==6900` בפועל,
לא הרשימה הידנית) העלתה **14 סניפי שופרסל** בכפר סבא, לא 6: `140, 144, 171,
230, 259, 335, 36, 375, 394, 615, 648, 682, 752, 845`. הרשימה הידנית
המקורית (144, 394, 615, 682, 752, 845) פספסה 8 סניפים אמיתיים — כולל
לפחות סניף בפורמט "יוניברס" (יוניברס גזית כ"ס-ויצמן) שלא זוהה בכלל בסבב
הראשון. זו בדיוק ההצדקה לבנות סינון לפי קוד רשמי מלכתחילה, לא רשימה
שמתבססת על קריאת שמות.

3 מתוך 14 הסניפים (615, 648, 682) חזרו עם 0 התאמות למוצר מפוקח בריצה הזו —
טרם נבדק אם זה אומר שאין להם PriceFull באותו יום, קטלוג חלב מצומצם, או
משהו אחר.

## שכבה 2-בקנה-מידה-עירוני: פער מחיר בין סניפי אותה רשת (`etl/scoring/cross_branch_spread.py`)

לא צריך רשת שנייה כדי שההשוואה תהיה משמעותית — שופרסל מפעילה בכפר סבא כמה
פורמטים (דיל/שלי, BE, אקספרס, יוניברס) במחירים שונים באופן משמעותי לאותו
ברקוד בדיוק. ריצה חיה ב-28.08.2026 מצאה 6,565 מוצרים שמופיעים ב-4 סניפים
לפחות עם פער מחיר בר-חישוב; הפער הגדול ביותר שנמצא: "מלון מובחר" ב-₪3.90
(יוניברס גזית כ"ס-ויצמן) מול ₪7.90 (אקספרס כפר סבא דוכיפת) — פער 102.6%
על אותו ברקוד, אותו יום.

## אתר סטטי (`etl/render/render_site.py` + `etl/render/*.py`, שכבה 4)

מרנדר את כל דפי `site/` ישירות מ-`GapResult`/`SpreadResult`/`StoreScore`
שכבר חושבו — אין שום חישוב או קריאת רשת בזמן ההגשה. מופעל אוטומטית מ-
`scripts/daily_snapshot.py` בסוף כל ריצה יומית. **הגשה בפועל (מתוקן
28.08.2026):** GitHub Pages עם `Source: GitHub Actions` (לא "Deploy from
branch" — השיטה הישנה תומכת רק ב-`/` או `/docs`, לא בתיקייה שרירותית
כמו `/site`). ה-workflow (`.github/workflows/daily-shufersal.yml`) מריץ
`configure-pages` + `upload-pages-artifact` + `deploy-pages` בסוף כל ריצה.

## גיאוקוד למפה — Nominatim (מאומת ותוקן, 28.08.2026)

`etl/enrich/geocode.py` — `nominatim.openstreetmap.org/search`, חינמי, בלי
מפתח, מוגבל ל-1 בקשה/שנייה. שאילתה **מובנית** (`street=`/`city=`/`country=`
נפרדים, לא מחרוזת טקסט אחת מחוברת) + `viewbox`+`bounded=1` סביב כפר סבא +
בדיקת שפיות על כל תוצאה (גם כשמגיעה מה-cache, כדי שתוצאה ישנה ושגויה
תתקן את עצמה בפעם הבאה ולא תישאר תקועה).

**באג אמיתי שנמצא ותוקן:** שאילתת טקסט חופשי בלבד גרמה לכתובות "קרובות
אבל לא מדויקות" (למשל סניף ברוטשילד שהוצג קרוב לוויצמן). **באג שני, חמור
יותר:** סניפים `615`/`140`/`259` שדה הכתובת שלהם ב-Stores.xml הרשמי היה
חסר/גנרי מדי (ריק, רק "כפר סבא", או עם סיומת "מתחם" שלא ניתנת לפרסור
כרחוב), וקרסו לאותה נקודת מרכז-עיר. **תוקן לא על ידי הסרת הסניפים** אלא
באיתור הכתובת האמיתית שלהם ידנית (חיפוש רשת, מאומת מול כמה מקורות
עצמאיים) ב-`etl/enrich/address_overrides.py`:
- `615` ("BE כפר סבא"): כצנלסון 14 — מאומת מול בסטור (bestore.co.il/online/he/branchs)
- `140` ("אקספרס תל חי"): תל חי 40 — מאומת מול easy.co.il ו-hasnif.co.il
- `259` ("יוניברס גזית כ"ס- ויצמן"): ויצמן 300 (בלי סיומת "מתחם G" שמבלבלת את הפרסר) — מאומת מול easy.co.il

## Leaflet + OpenStreetMap (מפה חיה, `etl/render/map.py`)

רעפי `tile.openstreetmap.org` הרגילים (חינמי, בלי מפתח, לא דורש הרשמה).
**ניסיון קודם** להחליף ל-CARTO (`basemaps.cartocdn.com`, לגרפיקה רזה יותר)
**נכשל בפרודקשן** — הציג ווטרמרק "API KEY REQUIRED" למרות בדיקות מקומיות
תקינות; הוחזר ל-OSM הרגיל, עם פילטר CSS על שכבת הרעפים למראה רזה יותר
במקום זאת. `maxZoom` מוגבל ל-16 כדי שמתאר בניינים בודדים לא יוצג.

## Open Food Facts (תמונות מוצר, `etl/enrich/product_images.py`)

`world.openfoodfacts.org/api/v2/product/{ברקוד}.json`, חינמי, בלי מפתח.
כיסוי חלקי (כ-23% מהמוצרים המוצגים בבנייה טיפוסית). **שני באגים אמיתיים
שנמצאו ותוקנו:** (1) חלק מהברקודים הלא-קיימים מחזירים 404 ישיר במקום
200+status:0 המתועד — מטופל כ"אין תמונה" כמו כל miss אחר; (2) קריאה של
כ-170 ברקודים ברצף בלי קצב פגעה ב-rate limit (429) — נוסף קצב מתון
(0.6 שניות) + retry עם backoff.

## התאמה דטרמיניסטית מול הבנצ'מרק (`etl/category_mapping/moag_matcher.py`)

ללא LLM, ללא תלות בפרויקט הקוד הפתוח (נבדק — אין שם פתרון לסיווג קטגוריית
מוצר, רק סיווג *סוג קובץ* וכלי נפרד להתאמת-מוצר-זהה-בין-רשתות). כלל: מילת
קטגוריה + אחוז שומן תואם + כמות/יחידה תואמת (כשמאומת) + היעדר מילות שלילה.

**ממצאים אמיתיים מהריצה החיה הראשונה, שהובילו לתיקוני קוד:**
- `\bגיל\b` (Python regex רגיל) *לא* מגן על "רגיל" כי ספרות נחשבות `\w` — כשל
  אמיתי, לא רק תיאורטי, על "חמוצה15%" (אין גבול-מילה בין ה' ל-1). נבנה פתרון
  ייעודי (`_hebrew_word_re`) שמתייחס לספרות/פיסוק כגבול חוקי.
- "גבינה לבנה 5% עיזים 250ג" (גבינת עיזים) התאים נגד בנצ'מרק גבינת פרה ויצר
  פער מדווח של +307% — false positive אמיתי, לא היפותטי. נוסף לרשימת השלילה.
- "מועשר"/"מועשרת" (מועשר) ו"עמיד" (UHT, לעומת "טרי" בבנצ'מרק) תומחרו
  15%-30% מעל הבנצ'מרק בריצה החיה — סווגו כווריאנטים שונים, לא פער אמיתי,
  ונוספו לרשימת השלילה.
- חלב: שם הפריט לא תמיד מציין "קרטון"/"שקית" (שני הבנצ'מרקים שונים במחיר) —
  כשלא ניתן לזהות, המערכת מחזירה את שני המועמדים ומסמנת "דו-משמעי" במקום
  לנחש (עקרון 2.1 בבריף: אין שדה שמייצג שיפוט).

## איסוף מקבילי (`etl/concurrency.py`, 28.08.2026)

`fetch_concurrently()` — `ThreadPoolExecutor` (I/O-bound, לא CPU-bound),
`max_workers=6` כברירת מחדל (נימוס מול פורטלים ציבוריים + הגנה מפני
rate-limit עצמי, לא רק מהירות). הורדת קבצי ה-PriceFull ב-`daily_
snapshot.py` עברה מטורית למקבילית. עוד לא הורחב לריצה מקבילית *בין*
רשתות שונות בו-זמנית — ייבנה באופן טבעי כשקרפור (הרשת השנייה) תתווסף,
כרגע יש רק שופרסל.

## קרפור — הרשת השנייה, חיה (`etl/scrapers/carrefour.py`, 28.08.2026)

**אין login, בניגוד ל-publishedprices.co.il.** דף הבית של `prices.carrefour.co.il`
מטעין ב-JS inline הכל מה שצריך: `const path = '20260828'` (תאריך היום),
`const files = [...]` (רשימת כל הקבצים, ללא pagination — כל הרשת הארצית
בעמוד אחד), `const branches = {...}` (מיפוי קוד סניף→שם). כתובת ההורדה
נבנית פשוט כ-`{origin}/{path}/{filename}` — אומת חי (הורדה אמיתית, גודל
תואם בדיוק ל-listing). 5 סניפים בכפר סבא (מאומת מול city_code=="6900" באותו
Stores file, אותו `kfar_saba_stores()` בדיוק): `010` (היפר, השקמה 21),
`400` (קניון, 20 כצנלסון), `404` (רון, 2 יעקב כהן), `471`/`473` (אונליין/
קוויק — ללא כתובת פיזית, שדה הכתובת ב-Stores.xml הוא URL).

**אותו schema XML בדיוק כמו שופרסל — אומת חי, לא הונח.** `shufersal.
parse_price_xml()` ו-`parse_stores_xml()` הורצו ישירות מול קובץ PriceFull
אמיתי של קרפור (12,461 פריטים, סניף 010) וקובץ Stores אמיתי (147 סניפים
ארצית, UTF-16 עם BOM — טופל אוטומטית ע"י `ET.fromstring`) — שניהם נפרסרו
נכון בלי שום שינוי קוד. `carrefour.py` לכן לא מגדיר פרסר משלו, רק
discovery/download, ומייבא את כל שאר הפונקציות משופרסל.

**באג אמיתי שנמצא ותוקן: קודי פריט "פנימיים" מתנגשים בין רשתות.**
`7290000000145` הוא "כרוב אדום" (₪6.90) אצל שופרסל, אבל "סלי אבל — חברת
מכבי" (סל ניחומים, ₪70.00) אצל קרפור, **באותו יום**. זה יצר פער מדווח של
914.5% שהיה הופך לכותרת הבית של האתר. הקוד תואם תבנית "729" + 7 אפסים +
מספר סידורי קצר — סימן מובהק לקוד PLU פנימי שכל רשת ממציאה בעצמה למוצרים
שקולים/ללא ברקוד יצרן אמיתי (ירקות בעיקר), לא ברקוד GS1 אמיתי. תוך כדי
הבדיקה נמצאו עוד 2 מקרים זהים בטופ-3 הפערים הגדולים ביותר (כרובית, חציל)
ו-80 מתוך 11,387 פריטים משותפים תואמים את התבנית החשודה. **תוקן ב-
`etl/scoring/item_code_filters.py`**: כל קוד שתואם את התבנית מוחרג מהשוואה
(גם `cross_branch_spread.py` וגם `store_ranking.py`) — לא כי הקוד הספציפי
הזה רע, אלא כי כל **צורת** הקוד הזו מעולם לא היתה מזהה אמין להשוואה חוצת-
רשתות (בתוך רשת אחת זה עדיין עקבי, כי כל רשת ממציאה אותו באופן פנימי
עקבי מול עצמה). **עודכן שוב אחרי שנוספה ויקטורי** — ראו למטה, אותה בעיה
בצורה שונה.

**ממצא נוסף:** `<StoreID>` בתוך ה-XML של קרפור **לא תמיד תואם** את קוד
הסניף בשם הקובץ/ב-listing (למשל קובץ עם שם סניף "404" מכיל בפועל
`<StoreID>113</StoreID>` בכל הרשומות). זה לא משפיע על שום דבר בקוד הפרויקט
(המפתח בכל מקום הוא תמיד המפתח החיצוני של ה-dict, לא השדה הפנימי של
הרשומה), אבל מתועד כאן כדי שלא יבלבל בדיבוג עתידי.

## ויקטורי — הרשת השלישית, חיה (`etl/scrapers/victory.py`, 28.08.2026)

**אין login.** בניגוד לניחוש המקורי (`laibcatalog.co.il/victory`, שהחזיר
403 — נתיב סטטי לא קיים), הפלטפורמה היא **API JSON אמיתי**: `GET /webapi/
api/getbranches?edi={chain_id}` ו-`GET /webapi/api/getfiles?edi={chain_id}`,
כתובת הורדה `{origin}/webapi/{chain_id}/{fileName}`. אומת מול קוד המקור
הפתוח של [israeli-supermarket-scarpers](https://github.com/OpenIsraeliSupermarkets/israeli-supermarket-scarpers)
(לא ניחוש) ואז מול הפורטל החי. **חשוב:** `getfiles` מחזיר את כל קטלוג
הרשת בכל מקרה, בלי קשר ל-`branchNumber` — קריאה אחת לרשת מספיקה, לא לולאה
per-branch (מתועד גם בקוד המקור של אותו פרויקט כתקלה שתפסו בעבר). מזהי
רשת (`chain_id`): ויקטורי `["7290696200003", "7290058103393"]`, מחסני השוק
`["7290661400001", "7290633800006"]`.

**סניף כפר סבא יחיד אומת חי:** `079` "כפר סבא הירוקה", אנגל 78 — תואם את
המחקר המקורי. **מחסני השוק: נבדק חי, אין אף סניף בכפר סבא** (71 סניפים
בסך הכל ברשימה החיה, אף אחד לא בעיר) — הערת התכנון הישנה ("לוי אשכול 37")
לא עמדה מול הנתונים החיים, ולכן לא נבנה scraper עבורה כרגע; אם ייפתח סניף
בעתיד, אותו קוד (`etl/scrapers/victory.py`) כבר תומך בה, רק להוסיף chain_id.

**אותו schema XML, עם 2 סטיות אמיתיות שתועדו:** (1) קובץ ה-Stores של
ויקטורי מאיית `<SubChainId>` (d קטנה) ולא `<SubChainID>` כמו שופרסל/קרפור
— `subchain_id` חוזר ריק, לא משפיע על שום דבר במורד הזרם. (2) **חשוב
יותר:** שדה `<City>` מכיל את **שם העיר** ("כפר סבא") ולא קוד יישוב מספרי
("6900") כמו אצל שופרסל וקרפור — `kfar_saba_stores()` ב-`shufersal.py`
עודכן להתאים לשתי הצורות (לא רק לנחש אחת).

**באג נוסף שנמצא ותוקן דרך ויקטורי: לא רק קודים "מלאים עם אפסים" מתנגשים.**
"2001" (אשכולית צהובה, ₪5.90 בויקטורי) התנגש עם מוצר לא קשור בקרפור
(₪55.00) — פער מדווח של 832.2%, **הכותרת הבאה של דף הבית** אחרי שהתקלה
הקודמת תוקנה. זה קוד PLU קצר (4 ספרות בלבד), לא ארוך-עם-אפסים כמו המקרה
של קרפור — תבנית שונה לגמרי מאותה בעיה בסיסית. **התיקון הוכלל מחדש**
ב-`etl/scoring/item_code_filters.py`: כל קוד קצר מ-8 ספרות (אורך ברקוד
EAN-8 המינימלי האמיתי) נפסל, בנוסף לתבנית "729+אפסים" הקודמת — שני כללים
נפרדים לשני ביטויים שונים של אותה בעיה עקרונית (קוד שממציאה רשת בעצמה,
לא GS1 אמיתי).

## ויקטורי חסומה מ-GitHub Actions — אומת מהלוג האמיתי, לא ניחוש (28.08.2026)

שתי ריצות רצופות של ה-workflow נכשלו על ויקטורי בלבד (שופרסל וקרפור עבדו),
כשאותו קוד בדיוק רץ נקי כל פעם מרשת ביתית. GitHub חוסם צפייה בלוגים גם
בריפו ציבורי בלי התחברות — המשתמש (מחובר לחשבון שלו) שלף את הלוג בעצמו
והדביק אותו. **הממצא האמיתי:**

```
urllib3.exceptions.ConnectTimeoutError: ... Connection to
laibcatalog.co.il timed out. (connect timeout=60)
```

זה timeout **ברמת ה-TCP connect עצמו** — לא 403, לא תגובת HTTP כלשהי,
ה-handshake פשוט לא מסתיים. **זה שולל את התיאוריה הקודמת** (חסימת
User-Agent) — כותרת HTTP לא יכולה לעזור כשהחיבור לא מגיע לשלב שליחת
הבקשה בכלל. ההסבר הסביר ביותר: `laibcatalog.co.il` (רשת קטנה יותר,
כבר מתועדת למעלה כלא-יציבה בעבר) חוסמת access ברמת ה-firewall לכל טווח
ה-IP של GitHub/Azure — לא נדיר אצל אתרים קטנים שמתגוננים מסריקות. **זה
מחוץ לטווח מה שתיקון בקוד לקוח יכול לפתור.**

**תוקן בפועל:** ה-timeout של ויקטורי ירד מ-60 שניות ל-`(10, 30)` (connect,
read) כדי שחסימה אמיתית תתגלה תוך שניות, לא תבזבז 5 דקות בכל ריצה יומית
בניסיונות retry על חיבור שלא יצליח בכל מקרה. `_safe_collect()` ב-
`daily_snapshot.py` כבר מבודד את הכשל — ויקטורי פשוט חסרה מהבנייה של אותו
יום, שאר האתר ממשיך לעבוד. אם החסימה תוסר אי-פעם מצד `laibcatalog.co.il`,
הקוד כבר מוכן בלי שינוי נוסף.

## איסוף מקבילי חוצה-רשתות (`scripts/daily_snapshot.py`, 28.08.2026)

עם 3 רשתות, שלב ההורדה (לא שלב ה-listing, ששונה מדי בין רשתות לאיחוד קל)
עבר מ-block טורי לכל רשת (מחכה שהרשת הקודמת תסיים לגמרי) לרשימת משימות
אחת משותפת לכל 3 הרשתות יחד, מורצת ב-`fetch_concurrently()` קריאה אחת —
כך שרשת שנייה/שלישית לא מחכה לראשונה. ראו גם `docs/sources.md` § "איסוף
מקבילי" למעלה (הרחבת הפגינציה של שופרסל עצמה, שם כבר תועד קודם).

## publishedprices.co.il — אין צורך בהרשמה, זה FTP אנונימי-למחצה (`etl/scrapers/publishedprices.py`, 28.08.2026)

הטבלה למעלה סימנה רמי לוי/יוחננוף/אושר עד כ"login: כן" על בסיס הפורטל
ה-HTTP ב-`https://url.publishedprices.co.il` — שם יש טופס "Request an
Account" אמיתי (נבדק חי בדפדפן: שם משתמש, שם פרטי/משפחה, אימייל, טלפון,
"Justification", וסיסמה — **בבדיקה אדמיניסטרטור לפני אישור**, לא הרשמה
מיידית). זה מה שהמשתמש התחיל לברר בצ'אט נפרד.

**אבל זה לא המנגנון היחיד, ואולי לא הנכון.** קריאת קוד המקור (לא ניחוש) של
`OpenIsraeliSupermarkets/israeli-supermarket-scarpers` — פרויקט open-source
פעיל שכבר מוזכר למעלה, מכסה 30+ רשתות — חשפה ש-`cerberus.py` (המנוע ששרתי
`publishedprices.co.il` רצים עליו) לא ניגש ל-HTTP בכלל: הוא מתחבר ב-**FTP**
לכתובת **אחרת**, `url.retail.publishedprices.co.il` (שימו לב ל"retail." —
host שונה מזה שבטבלה למעלה), עם שם משתמש קבוע לכל רשת **וסיסמה ריקה**:

```python
# il_supermarket_scarper/scrappers/ramilevy.py, yohananof.py, osherad.py
RamiLevi  / chain_id 7290058140886
yohananof / chain_id 7290803800003
osherad   / chain_id 7290103152017
```

**אומת בפועל בסשן הזה** (לא רק הועתק מהמקור), עם שני clients עצמאיים
(Python `ftplib` וגם `curl`), נגד השרת האמיתי:

```
ftp.login(user="RamiLevi", passwd="")   -> 230 Password Ok, User logged in
ftp.login(user="yohananof", passwd="")  -> 230 Password Ok, User logged in
ftp.login(user="osherad", passwd="")    -> 230 Password Ok, User logged in
```

כלומר: **אין צורך בהרשמה בכלל** לשלושת הרשתות האלה — הטופס המנוהל-אדמין
בפורטל ה-HTTP הוא מנגנון נפרד, לא הדרך שרשתות המחיר האמיתיות משתמשות בה
כדי לפרסם נתונים (החוק מחייב פרסום ציבורי חופשי — וזה בדיוק מה שה-FTP הזה
הוא).

**מה שלא אומת (חסום, לא דולג):** ה-sandbox של הסשן הזה לא מצליח לפתוח שום
חיבור-דאטה FTP יוצא — נבדק כללית, לא רק נגד השרת הזה: אותו timeout בדיוק
קורה גם מול `ftp.debian.org`, ואפילו התחברות גולמית ל-FTP host לא-קשור
בפורט 21 פשוט נתקעת. ערוץ הבקרה (login) עובד מצוין על אותו חיבור בדיוק —
רק ערוץ הדאטה (LIST/RETR, גם passive וגם active) לא מסתיים אף פעם. כתוצאה:
- צורת ה-listing/שמות-קבצים המדויקת בשרת הזה **מניחה** (לא מאמתת) שהיא
  זהה למוסכמה שכבר אומתה עבור שופרסל/קרפור/ויקטורי (אותה תקנה, אותו schema
  בכל פעם עד כה).
- מספרי הסניפים בכפר סבא לשלוש הרשתות האלה **לא ידועים** — אפשר לקבל אותם
  רק מקובץ Stores אמיתי שהורד בפועל.
- האם `shufersal.parse_price_xml()`/`parse_stores_xml()` עובדים בלי שינוי
  גם כאן (כמו שקרה לקרפור וויקטורי) — **לא מאומת** מאותה סיבה.

GitHub Actions לא צפוי לסבול מהחסימה הזו (גישת רשת יוצאת רגילה, לא
sandbox מוגבל) — ההפך המדויק ממצב ויקטורי (שם GitHub נחסם ורשת ביתית
עבדה; כאן ה-sandbox הזה חסום ו-GitHub צפוי לעבוד). זו ציפייה שדורשת ריצת
CI אמיתית כדי להתאמת, לא ניחוש נוסף.

**נבנה בפועל:** `etl/scrapers/publishedprices.py` — login/list/download
דרך `ftplib`, מוסכמת שמות-קבצים זהה לקרפור (regex מוכח), קונפיגורציית
3 הרשתות עם ה-usernames/chain_id המאומתים למעלה. **לא נבנה עדיין** (בכוונה,
לא נשכח): `kfar_saba_stores()`/מספרי סניפים אמיתיים לשלוש הרשתות — דורש
קובץ Stores אמיתי שהורד בהצלחה, וזה כרגע לא אפשרי מה-sandbox הזה.
`tests/test_publishedprices.py` מכיל רק טסטים אופליין (parsing לוגיקה) —
לא live tests כמו שאר הרשתות, מהסיבה שתועדה למעלה.

## מיפוי מלא של פלטפורמות משותפות + Health check (28.08.2026)

המשתמש ביקש שתי הרחבות לפני שממשיכים: (א) health-check שרץ **לפני** ניסיון
איסוף אמיתי — אם המקור לא זמין, לא לצאת אליו בכלל; (ב) לברר כמה רשתות
נוספות רצות על אותה תשתית משותפת שכבר בנינו (Cerberus/FTP), כדי למקסם כיסוי
ביעילות.

**Health check (`etl/health_check.py`, חדש):** `ftp_preflight()` — login +
פעולת LIST מוגבלת (`nlst("*store*")`) עם timeout קצר, לפני כל ניסיון איסוף
אמיתי. **קריטי, לא מובן מאליו:** בדיקה שמסתפקת ב-login בלבד הייתה **פספסת
בדיוק את התקלה שכבר קרתה כאן** — ה-login עצמו הצליח (ראו סעיף קודם), רק
ערוץ הדאטה נתקע. הבדיקה חייבת לתרגל את הפעולה שבאמת נכשלת, לא רק את
ה-handshake. `etl/scrapers/publishedprices.py` מקבל פונקציית `preflight()`
שעוטפת את זה; כל אינטגרציה עתידית ל-`daily_snapshot.py` אמורה לקרוא לה
לפני איסוף אמיתי ולדלג לגמרי אם היא מחזירה `False`.

**מיפוי מלא של Cerberus (`OpenIsraeliSupermarkets`, קריאת קוד מקור, לא
ניחוש):** 12 רשתות רצות על הפלטפורמה הזו בפועל (לא 3): רמי לוי, יוחננוף,
אושר עד, **טיב טעם**, **דור אלון (AM:PM)**, **Yellow**, ובנוסף (ללא סניף
ידוע בכפר סבא): Fresh Market/סופר דוש, קשת תימנים, פוליצר, סלאח דבאח,
Stop Market. **שלושה מהם דורשים סיסמה אמיתית, לא ריקה** (מפורסמת בקוד
הפתוח לצורך קריאת הנתונים הציבוריים האלה — לא מידע רגיש שהפרויקט הזה גילה
או עוקף): Yellow (`Paz_bo`/`paz468`), Salach Dabach (`SalachD`/`12345`),
Super Yuda (`yuda_ho`/`Yud@147`, path `/Yuda`, נבדק — **אין** לו סניף בכפר
סבא, מבוסס תל אביב).

**מיפוי פלטפורמת Bina (binaprojects.com) — לא נבדקה קודם, נמצאה תוך כדי
המיפוי:** עוד פלטפורמה משותפת, **ללא login אישי** (session מבוסס cookies
בלבד, לא username/password) — מכסה בין השאר את קינג סטור, זול ובגדול,
מעיין 2000, סופר ספיר, שפע ברכת השם, City Market (3 ערים), **ו-שוק העיר**
(שכבר היה סעיף פתוח בתוכנית — נסגר עכשיו).

**כפר סבא בפועל, מאומת בחיפוש חי (לא רק שם הרשת ברשימה):**
- **טיב טעם** — סניף אמיתי, רחוב נעמי שמיר 15, מתחם LEVINTECH (סניף עירוני
  ראשון של הרשת בעיר).
- **דור אלון / AM:PM** — סניף אמיתי, צ'רניחובסקי 24, פתוח 24/7.
- **Yellow** — סניף אמיתי, התע"ש 32 (מכולת נלווית לתחנת דלק).
- **קינג סטור** (Bina) — כתבה מקומית מאשרת פתיחת סניף בכפר סבא.
- **שוק העיר** (Bina) — סניף אמיתי, אלי הורביץ.
- נבדקו ונפסלו (שם דומה, לא רלוונטי בפועל): בראקת = חנות כלים/חומרה, לא
  מזון; זול ובגדול = מרוכז ירושלים/אשדוד-אשקלון, אין סניף בעיר; סופר יודה =
  מבוסס תל אביב, אין סניף בעיר.

**פולבק לוויקטורי/מחסני השוק — נבדק, לא קיים:** יש להם גם scraper מבוסס
מנוע "Matrix" ישן ב-`OpenIsraeliSupermarkets`, אבל הוא **פונה לאותו host
בדיוק** (`laibcatalog.co.il`, רק עמוד ASPX שונה) — לא host עצמאי. חסימת
ה-TCP שכבר אובחנה שם (ראו סעיף ויקטורי למעלה) צפויה לחסום גם את זה באותה
צורה. אין פולבק אמיתי דרך המקור הזה.

**המצב אחרי המיפוי:** `etl/scrapers/publishedprices.py`'s `CHAINS` עודכן
מ-3 ל-6 רשתות (הוספת טיב טעם/דור אלון/Yellow, עם תמיכה בסיסמה לכל רשת).
Bina עדיין **לא נבנה** — זו התשתית השנייה (מקבילה ל-Cerberus) שתפתח את
קינג סטור + שוק העיר במכה אחת, בלי login, ברגע שיש אישור להמשיך. עם שתי
התשתיות האלה בלבד (Cerberus שכבר בנוי, Bina שעדיין לא) — כל 12 הרשתות
הידועות עם סניף אמיתי בכפר סבא מכוסות, בלי שום adapter נוסף מעבר לשתיים
שכבר קיימות/מתוכננות.

## סבב שני: בדיקה מול gov.il עצמו, לא רק מול פרויקט הרפרנס (28.08.2026)

המשתמש ביקש לוודא שלא סומכים רק על כיסוי פרויקט הרפרנס (`OpenIsraeliSupermarkets`)
— הוא צד שלישי, לא המקור הרשמי, ועלול לפגר אחרי רשתות חדשות. הרצת מחקר
נפרדת (agent) קראה ישירות מ-https://www.gov.il/he/pages/cpfta_prices_regulations
(29 רשתות רשומות, מעודכן ל-12.08.2026 — אין הרחבה/צמצום מהבדיקה הקודמת)
ובדקה כל רשת שלא הייתה מסומנת כ"נבדקה" מול חיפוש חי לכתובת אמיתית בכפר סבא.

**4 רשתות נוספות אומתו — כולן כבר על Cerberus שכבר בנוי, רק לא ידענו שיש
להן סניף בעיר:**
- **Stop Market** — מתחם איינשטיין (`ftp_username="Stop_Market"`)
- **Fresh Market** — צ'רניחובסקי 16, מתחם G (`ftp_username="freshmarket"`)
- **קשת טעמים** — נתיב האבות 14, מרכז שרונה (`ftp_username="Keshet"`)
- **סאלח דבאח** (מותג צרכני "Angus") — G Mall, ויצמן 207
  (`ftp_username="SalachD"`, סיסמה `12345` — לא ריקה)

ארבעתן נוספו ל-`CHAINS` ב-`etl/scrapers/publishedprices.py` — עכשיו 10
רשתות, לא 6. **עלות שולית אפסית**: אותו client, רק שורה נוספת בdict.

**רשת חדשה, פלטפורמה נפרדת:** **City Market** — ויצמן 55, פורטל עצמאי
(`citymarket-shops.co.il`), **לא** על Cerberus/Bina — דורש אינטגרציה
נפרדת (מחקר read-only + פרסר, כמו קרפור בזמנו). לא נבנה עדיין.

**שתי החלטות סקופ פתוחות, לא הוכרעו באופן חד-צדדי:**
- **Good Pharm ו-Super-Pharm** (בתי מרקחת) — יש להן סניף אמיתי בעיר וכיסוי
  משפטי (החוק חל על "ענפי המזון **והפארם**"), אבל הן רשתות פארם, לא סופר —
  הרלוונטיות למחירי חלב/מוצרי יסוד מוטלת בספק. לא נבנה, ממתין להחלטה.
- **Wolt Market** — dark store למשלוחים בלבד תחת `wm-gateway.wolt.com`
  (פלטפורמה חדשה, לא Bina/Cerberus/laibcatalog). יש לו נוכחות אמיתית
  בכפר סבא, אבל **אין לו כתובת פיזית שאפשר לנווט אליה** — לא מתאים
  למתודולוגיית "פין על מפה + כפתור וויז" כפי שהאתר בנוי היום. לא נבנה.

**סה"כ אחרי שני הסבבים:** 17 רשתות עם סניף אמיתי מאומת בכפר סבא (היה 7
בתחילת השיחה הזו) — 10 מהן על Cerberus (בנוי), 2 על Bina (King Store,
שוק העיר — לא נבנה עדיין), Shufersal+Carrefour על פורטלים עצמאיים (בנוי),
Victory+מחסני השוק על laibcatalog.co.il (בנוי, חסום מ-CI), City Market
על פורטל עצמאי נפרד (לא נבנה), + שתי רשתות פארם ממתינות להחלטת סקופ.

## פולבק ליום קודם כשמקור שלם לא זמין (`etl/raw_snapshot_fallback.py`, 28.08.2026)

המשתמש ביקש שני דברים לגבי ויקטורי: (א) לבדוק אם ל-API של laibcatalog.co.il
יש היסטוריה של ימים קודמים; (ב) אם לא, לבנות מנגנון שנופל חזרה ליום הקודם
כשאין מחיר של היום.

**נבדק חי, לא ניחוש:** `getfiles` על laibcatalog.co.il מחזיר **רק** את
קבצי היום הנוכחי — 1,816 קבצים, כולם עם `fileDate` של אותו יום בדיוק.
ניסיתי חמישה שמות פרמטר סבירים לתאריך (`date`, `day`, `fileDate`, `d`,
`from=20260827`) מול chain_id אמיתי של ויקטורי — **כולם התעלמו מהפרמטר**
(אותה תגובה, אותם 1,816 קבצים, כולם מתאריך היום) — לא "לא נתמך", אלא
מאומת שהפרמטר פשוט מתעלם. **המסקנה: אין היסטוריה ב-API הזה בכלל.** המקום
היחיד שיכול להכיל "אתמול" הוא הפרויקט עצמו — וזה כבר קיים: כל ריצה
מוצלחת כותבת XML גולמי ל-`data/raw/<תאריך>/`, שכבר מחויב (commit) יומית.

**נבנה בפועל:**
- `etl/raw_snapshot_fallback.py` — `find_fallback_catalogs()`: כשרשת חוזרת
  עם אפס קבצים היום (`_safe_collect` כבר תופס את זה), מחפש אחורה עד 7 ימים
  ב-`data/raw/` את ה-XML הגולמי האחרון שכן קיים לכל סניף ידוע, פר-סניף
  בנפרד (לא מניח שכל הסניפים של רשת ייפלו לאותו יום, גם אם זה המצב הנפוץ).
- `etl/enrich/store_directory.py` — directory "דביק" (`store_directory.json`,
  אותו דפוס self-healing-cache כמו geocode/תמונות): שם וכתובת של סניף
  נשמרים בכל ריצה מוצלחת, ולא נמחקים כשסניף חסר מריצה נתונה — כי אם רשת
  שלמה נכשלת (Stores file כולל), אין מאיפה לדעת את השם/כתובת של היום בלי
  זה.
- `etl/scrapers/victory.py`: נוסף `KFAR_SABA_STORE_IDS = {"079"}` (אותו
  תפקיד כמו הקבוע המקביל שכבר קיים ב-`carrefour.py`) — כי אם ה-listing
  החי נכשל לגמרי (בדיוק המקרה הזה), אין Stores file לגזור ממנו את מספרי
  הסניפים.
- `etl/render/store.py`: פרמטר `as_of_date` חדש ל-`render_store_html()` —
  כשמוגדר, מציג chip כתום גלוי בראש דף הסניף: "המחירים כאן מעודכנים
  ל-[תאריך] — המקור לא היה זמין היום". **לא מוצג בשקט כאילו זה מחיר של
  היום** — עקבי עם עיקרון הכנות של האתר (טראק 0 בתוכנית המקורית קבע כלל
  דומה לגבי אי-ודאות גיאוקוד).
- `scripts/daily_snapshot.py` מחובר: כל רשת עם אפס קבצים חיים היום בודקת
  fallback (משתמשת בקבועי ה-fallback הקיימים של כל רשת: Shufersal/
  Carrefour/Victory כולן כבר יש להן), מוסיפה את הקטלוגים למאגר הראשי,
  ומעדכנת את ה-directory. **לא כותב מחדש** את ה-XML של אתמול תחת תיקיית
  היום — `data/raw/<תאריך>/` נשאר תיעוד אמיתי של מה שבאמת נאסף באותו יום.

**אומת:** 79 טסטים עוברים (כולל 4 חדשים ל-fallback + 3 ל-store_directory,
עם תיקיות זמניות אמיתיות, לא mock), והרצה מקומית מלאה של `daily_snapshot.py`
(שלוש הרשתות הצליחו היום מהרשת המקומית — ה-fallback לא הופעל בפועל, אבל
אימת שהחיווט לא שבר את המסלול הרגיל). **לא אומת עדיין:** ריצה אמיתית שבה
רשת נכשלת בפועל ומפעילה את הנתיב — זה קורה רק כשוויקטורי אכן חסום (כלומר
בפועל ב-GitHub Actions), אז יתגלה ויתועד בריצה היומית האמיתית הבאה.

## Bina — הפלטפורמה השנייה, חיה (`etl/scrapers/bina.py`, 28.08.2026)

נבנה מיד אחרי המיפוי למעלה, לפי אותו תהליך כמו קרפור בזמנו: מחקר read-only
קודם (קריאת `engines/bina.py` המלא מ-`OpenIsraeliSupermarkets`), בדיקה חיה
לפני כתיבת פרסר, ורק אז קוד.

**מבנה הבקשה, מאומת חי נגד King Store ו-שוק העיר:**
- Listing: `GET {base}/MainIO_Hok.aspx?_={chain_id}&wReshet=הכל&WFileType=&WDate=&WStore=`
  — מחזיר **JSON**, לא HTML/טבלה כמו שופרסל.
- הורדה היא **redirect דו-שלבי**, לא URL ישיר — קל לפספס בלי לבדוק חי:
  `GET {base}/Download.aspx?FileNm={filename}` מחזיר JSON `[{"SPath": "..."}]`,
  ורק את ה-URL הזה מורידים בפועל.
- אותו schema רגולטורי כמו כל שאר הרשתות — `parse_price_xml`/
  `parse_stores_xml` של שופרסל עובדים בלי שינוי (אומת מול קובץ PriceFull
  אמיתי, 1,938,656 בייט אחרי פתיחה, וקובץ Stores אמיתי, 26 סניפים).
- שם קובץ עם סיומת `.GZ` **גדולה** (לא `.gz` כמו שופרסל/קרפור) — טופל
  case-insensitive, לא הונח.

**כפר סבא, מאומת מקובץ Stores אמיתי, לא מכתבה בעיתון:**
- **שוק העיר: כן** — סניף יחיד, `011`, "כפר סבא מזרח", אלי הורביץ 26
  (סינון City רשמי, קוד 6900). **נבנה ומחובר בפועל.**
- **קינג סטור: לא** — כתבה מקומית (kfarsaba.mynet.co.il) דיווחה על
  "פתיחת סניף", אבל קובץ ה-Stores האמיתי (29 סניפים, נבדק 28.08.2026)
  לא מראה שום סניף עם קוד היישוב הרשמי של כפר סבא. **לא נכלל** — עדיף
  חסר מאשר שגוי; אפשר לבדוק שוב מאוחר יותר.

**באג כתובת נוסף שנמצא ותוקן:** `Stores.xml` של שוק העיר מאיית "אלי
**הורוביץ**" — איות שלא קיים ב-OSM כלל (0 תוצאות ב-Nominatim). האיות
הנכון "אלי **הורביץ**" (וגם "Eli Hurvitz" באנגלית) נותן תוצאה נקייה
ואחת, באזור התעשייה של כפר סבא — אותו רחוב, לא מיקום אחר. נוסף
ל-`ADDRESS_OVERRIDES` (`etl/enrich/address_overrides.py`).

**חובר בפועל:** `scripts/daily_snapshot.py` מקבל `_collect_shuk_hair()`
(אותו דפוס בדיוק כמו `_collect_victory()`), עם namespace `shukhair-`,
ומחובר גם למנגנון ה-fallback (`bina.SHUK_HAIR_KFAR_SABA_STORE_IDS`).

**אומת חי, בדפדפן, לא רק "אמור לעבוד":** הרצה מקומית מלאה — 5,849 פריטים
אמיתיים מ-שוק העיר, 13 מתוך זה תואמים לבנצ'מרק החלב. דף הסניף (`/store/
shukhair-011/`) מציג השוואות אמיתיות חוצות-רשתות (למשל שמן גרעיני ענבים:
₪32.90 בשוק העיר מול ₪54.90 ביוניברס גזית, פער 67%-). לפני תיקון הכתובת:
17 פינים על המפה; אחרי: 18 (הפין של שוק העיר מופיע נכון). 84 טסטים עוברים.

## 10 רשתות Cerberus מחוברות בפועל + באג אמיתי שנתפס ב-production (28.08.2026)

**חובר:** `_collect_cerberus_chain()` גנרית אחת לכל 10 הרשתות (לא 10 פונקציות
כמעט-זהות — כולן חולקות client אחיד), עם `preflight()` קודם לכל ניסיון,
ורצות **במקביל** (לא ברצף, 10 חיבורי FTP סדרתיים היו מיותרים). אומת מקומית:
כל 10 נכשלות ב-preflight תוך שניות (אותה חסימת FTP של ה-sandbox), שאר האתר
ממשיך לעבוד נכון — אבל **האם הן באמת עובדות** לא ניתן לאמת מקומית (דורש
חיבור FTP עובד). זה בדיוק המקרה ש"תוודא" ביקש לבדוק.

**נדחף, ונבדק בפועל מול ריצת GitHub Actions אמיתית** (run #14, לא רק הנחה
שזה יעבוד): הריצה הצליחה (`Status: Success`), אבל בדיקה של האתר החי גילתה
**באג אמיתי** — `/store/victory-079/` חזר 404, למרות שוויקטורי כבר עבד
בהצלחה קודם היום. `git diff` של קומיט הבוט אישר: `delete mode ... site/
store/victory-079/index.html` — הפין נעלם.

**האבחנה, מאומתת מול הנתונים האמיתיים שהיו ב-repo:** ה-workflow עכשיו רץ
על **כל push**, לא רק פעם ביום (ראו `.github/workflows/daily-shufersal.yml`)
— אז "היום" יכול לכלול כמה ריצות. ויקטורי הצליח בריצה מוקדמת יותר היום
(`data/raw/2026-08-28/PriceFull7290696200003-...-079-...xml` היה כבר
ב-repo, מאומת עם `git log`), אבל נכשל בריצה מאוחרת יותר (חסימת ה-CI
המוכרת) — וה-fallback שבניתי **לא מצא את זה**, כי `find_fallback_catalogs()`
חיפש החל מ"אתמול" בלבד (`before - 1 day`), במפורש **לא** כולל את `before`
(היום) עצמו — הנחה שגויה שהייתה נכונה רק כשיש ריצה אחת ביום.

**תוקן:** הטווח שונה מ-`range(1, N)` ל-`range(0, N)` — מחפש קודם ב"היום"
עצמו (שעשוי כבר להכיל תוצאה מריצה מוקדמת יותר), ורק אז נסוג אחורה. אומת
נגד **הנתונים האמיתיים** שנמשכו מה-repo (לא רק temp dir סינתטי): לפני
התיקון `victory-079` לא נמצא; אחרי התיקון — 8,948 פריטים אמיתיים נמצאים,
`as_of=2026-08-28`. גם תוקנה הודעת ה-chip בדף הסניף: הניסוח הקודם
("המקור לא היה זמין היום") היה סותר את עצמו כש-`as_of_date` שווה בדיוק
להיום — נוסח מחדש לניטרלי ("מהאיסוף האחרון שהצליח, X") שנכון גם כש-X
הוא היום עצמו וגם כשהוא יום קודם. 85 טסטים עוברים.

**לקח:** זו בדיוק הסיבה ש"תוודא" חשוב — הבדיקה המקומית (temp dirs
סינתטיים) עברה בהצלחה, אבל פספסה הנחה סמויה על "כמה ריצות יש ביום" שרק
ריצת production אמיתית חשפה.

## City Market — נבדק חי ונפסל: הפורטל עובד, המקור עצמו שבור (28.08.2026)

בדיקה read-only לפני קוד (כמו תמיד): הפורטל `citymarket-shops.co.il` באמת
חי, ללא login, ויש בו dropdown עם **כל** הסניפים כולל כתובת מלאה בטקסט —
מצאתי ישירות "066 | סיטי מרקט וייצמן כפר סבא, וייצמן 55" — תואם בדיוק את
מה שנמצא קודם בחיפוש חי. עד כאן הכל תקין.

**אבל בדיקת התוכן בפועל חשפה שהמקור עצמו שבור, לא בעיית פרסור:**
- קובץ ה-PriceFull הגדול ביותר של סניף 066 (25MB, 37,761 פריטים) — **כל
  שדה `<ItemPrice>` ריק** (`<ItemPrice/>`) בכל הקובץ, בלי יוצא מן הכלל.
  אין שום מחיר בקובץ שלם.
- `<ChainId/>`, `<SubChainId/>`, `<StoreId/>` **ריקים** בקובץ עצמו — הקובץ
  לא מזהה את עצמו בכלל.
- `<PriceUpdateDate>` בפריטים בודדים מציג **2021-04-27** — בתוך קובץ
  שה-metadata שלו טוען שהוא מ-28.08.2026. נתונים מיושנים/placeholder,
  לא מחירים אמיתיים נוכחיים.
- קובץ ה-Stores הרשמי: `<Address>unknown</Address>`, `<City>unknown</City>`
  לכל סניף בלי יוצא מן הכלל — אין שדה City רשמי לסנן לפיו (הכתובת קיימת
  רק כטקסט חופשי בתוך `<StoreName>`).

**וידוא שזה לא ספציפי לפרסור/לסניף:** בדקתי סניף אחר (030) באותה רשת —
שם יש מחירים אמיתיים, chain_id תקין, 41,631 פריטים. כלומר הבעיה היא
ספציפית לסניף כפר סבא (066) של הרשת הזו, לא לפורטל כולו ולא לפרסר.

**מסקנה:** לא ניתן לבנות השוואת מחירים על נתונים שאין בהם מחירים —
זה לא משהו שתיקון קוד יכול לפתור, זה כשל איכות נתונים במקור עצמו.
**לא נבנה, בכוונה, לא נשכח** — אם המקור יתוקן אי-פעם מצד הרשת, כל
התשתית הקיימת (`shufersal.parse_price_xml`/`parse_stores_xml`) כבר
עובדת על סניפים אחרים באותה רשת, כך שזה יהיה קל להוסיף מחדש.

## למה 10 רשתות ה-Cerberus לא מביאות נתונים — מנגנון אבחון אמיתי, לא ניחוש (28.08.2026)

המשתמש ביקש "סקירה להבין מה הולך שם" במקום לקבל את ההנחה "כנראה אותה חסימה"
כמובנת מאליה. הבעיה: אין לי גישה ללוגים של GitHub Actions (נדרש login, גם
ברפו ציבורי — כבר אומת בעבר). **הפתרון:** קובץ שנכתב ע"י `daily_snapshot.py`
עצמו בכל ריצה (`data/processed/<תאריך>/cerberus_diagnostics.json`, מחויב
אוטומטית ע"י ה-workflow) — קובץ ב-repo ציבורי נגיש לקריאה גולמית **בלי**
login, בניגוד ללוג ריצה. `etl/health_check.ftp_preflight_diagnostic()` חדש
מחזיר לא רק True/False אלא **באיזה שלב** נכשל (`connect`/`login`/`list`)
והחריגה האמיתית, לכל אחת מ-10 הרשתות בנפרד.

**ממצא מיידי מהרצה מקומית (עדיין לא מה-CI, אבל כבר גילה משהו אמיתי):**
9 מתוך 10 נכשלות ב-`list` עם `TimeoutError` (תואם את חסימת ערוץ-הדאטה
שכבר אובחנה) — **אבל דור אלון נכשלה ב-`login` עם `error_perm: 530 Secure
connection required`**, שגיאת FTP אמיתית, לא timeout. זה אומר שהחשבון
הזה דורש **FTPS** (FTP מוצפן), לא FTP רגיל — דרישה שונה לגמרי, לא עוד
מקרה של אותה חסימה.

**אומת חי:** ניסיון FTP רגיל מול דור אלון נכשל עם אותה 530; מעבר ל-
`ftplib.FTP_TLS` (+ `prot_p()` להצפין גם את ערוץ הדאטה) — **ה-login
מצליח**. תוקן בקוד: `CHAINS["dor-alon"]["use_tls"] = True`, פרמטר
`use_tls` זורם עכשיו דרך `_connect`/`list_files`/`download`/`preflight`
ב-`publishedprices.py` וב-`daily_snapshot.py`. אחרי התיקון: דור אלון
מגיעה עכשיו לשלב `list` (כמו כל השאר) במקום להיכשל מוקדם יותר ב-`login`
— התקדמות אמיתית ומאומתת, גם אם ערוץ-הדאטה עצמו עדיין חסום מה-sandbox
הזה. 91 טסטים עוברים.

**מה שזה עדיין לא עונה:** האם ערוץ-הדאטה חסום גם מ-GitHub Actions או רק
מה-sandbox הזה — זו בדיוק השאלה שהקובץ המחויב הזה נועד לענות עליה, ברגע
שריצת CI אמיתית תכתוב את הגרסה שלה של `cerberus_diagnostics.json`.

**התשובה הגיעה — נקראה ישירות מ-`raw.githubusercontent.com` אחרי ריצת
CI אמיתית (run #18, commit `66f1517`), לא ניחוש:**

```
כל 10 הרשתות: "failed_at": "list", "error": "TimeoutError: timed out"
כולל dor-alon (שעכשיו כן עוברת login בהצלחה בזכות התיקון — ראו למעלה)
```

**מסקנה סופית, מאומתת משני סביבות ענן עצמאיות (ה-sandbox הזה **וגם**
GitHub Actions עצמו):** ערוץ-הדאטה של `url.retail.publishedprices.co.il`
חסום לגמרי — לא רק מה-sandbox המקומי. תיקון ה-TLS **עבד** (דור אלון עוברת
login נקי בשני הסביבות עכשיו), אבל זה לא פותר את זה — חסימת ערוץ-הדאטה
יושבת מתחת ל-login, וקיימת בשני מקומות בו-זמנית. הדפוס הסביר ביותר: השרת
הקטן הזה חוסם טווחי IP של ספקי ענן (Azure/AWS/GCP) גורף — בדיוק כמו
`laibcatalog.co.il` (ויקטורי) שכבר תועד קודם באותו דפוס בדיוק, רק שם
ברמת TCP connect ולא ברמת ה-listing.

**המשמעות המעשית:** 10 הרשתות האלה **לא צפויות לעבוד** מ-GitHub Actions
בעתיד הנראה לעין, לא כי הקוד שבור אלא כי המקור עצמו חוסם תשתית ענן —
בדיוק כמו ויקטורי. הקוד נשאר מוכן ומבודד (`_safe_collect` + health check
מונעים מזה לפגוע בשאר האתר), וימשיך לנסות כל יום — אם החסימה אי-פעם
תוסר מצידם זה יעבוד בלי שינוי נוסף. לא נבנה שום workaround לעקוף חסימת
IP (למשל proxy בתשלום) — מחוץ לעקרון ₪0/בלי שירותי צד-שלישי של הפרויקט,
ולא התבקש.

## פריצת דרך: אין צורך "לפתוח" את החסימה — יש דרך HTTPS שלא נחסמת בכלל (28.08.2026)

המשתמש ביקש טבלה של מה לעשות כדי "לפתוח" את החסימה, כולל אם צריך להירשם.
לפני שבניתי טבלה — בדקתי אם יש בכלל צורך. **מצאתי דרך שלמה שעוקפת את
הבעיה לגמרי, בלי הרשמה, בלי לפתוח כלום, בלי לחכות לאף אחד:**

הפלטפורמה הזו ("Cerberus Web Client", מוצר FTP server מסחרי אמיתי של
NCR L.T.D — לא שם שהמצאנו) חושפת **גם** ממשק web רגיל ב-HTTPS, לא רק FTP
— ב-host **אחר** (`url.publishedprices.co.il`, בלי "retail.") שלא סובל
מאותה חסימה. **אותם שמות משתמש וסיסמאות בדיוק** שכבר משתמשים בהם ל-FTP
עובדים גם שם — בלי הרשמה, בלי אישור אדמין. אומת חי נגד 3 רשתות כולל שתיים
עם סיסמה אמיתית (Yellow, סאלח דבאח):

```
GET  /login              -> csrftoken ב-<meta>
POST /login/user         -> username+password+csrftoken -> מפנה ל-/file
GET  /file                -> csrftoken חדש (מתחלף בכל טעינת דף)
POST /file/json/dir      -> JSON עם כל הקבצים (jQuery DataTables API)
GET  /file/d/<filename>  -> הקובץ הגולמי (gzip ל-Price*, xml גולמי ל-Stores)
```

`iDisplayLength=100000` בקריאה אחת החזיר את **כל** 2,787 הקבצים של רמי לוי
— אין צורך ב-pagination בכלל בקנה המידה של הפרויקט הזה.

**המודול `etl/scrapers/publishedprices.py` נכתב מחדש לגמרי** — FTP הוסר
(`ftplib`/`use_tls` נעלמו), הכל עובר עכשיו דרך `requests` רגיל מול
`url.publishedprices.co.il`. `etl/health_check.py` (שנבנה ספציפית ל-FTP)
**נמחק** — קוד מת אחרי המעבר, לא נשאר "ליתר ביטחון".

**התוצאה, מאומתת בהרצה מקומית מלאה, לא תיאוריה:** כל 10 הרשתות עברו
login+listing בהצלחה (לא רק חלק). **5 מהן התגלו עם סניף אמיתי בכפר סבא
שלא היה קודם**:

| רשת | סניפים חדשים | פריטים |
|---|---|---|
| רמי לוי | 033, 719 | 14,576 + 4,306 |
| אושר עד | 031 | 6,833 |
| טיב טעם | 093 | 7,814 |
| דור אלון | 582, 958 | 5,290 + 1,217 |
| Yellow | 234, 235, 251 | 2,031 + 3,597 + 1,805 |

(יוחננוף, Stop Market, סאלח דבאח, Fresh Market, קשת — כולן הצליחו
login+listing, אבל **אין** להן סניף עם קוד היישוב הרשמי של כפר סבא —
תוצאה אמיתית מהבדיקה, לא כישלון גישה.)

**סה"כ: מ-21 סניפים ל-30**, מ-18 פינים על המפה ל-**22** (חלק מהכתובות
החדשות עדיין לא נמצאו ב-geocoding — אותה מגבלה ידועה כמו כל רשת חדשה
שנוספה עד כה, לא באג). דף הסניף החדש של רמי לוי בכפר סבא כבר מציג
השוואות אמיתיות מול שוק העיר (`shukhair-011`) — שתי רשתות "חדשות" באותו
יום מצטלבות זו עם זו. 84 טסטים עוברים, כולל טסטים חיים חדשים (לא mock)
שמוכיחים את כל הזרימה מול 3 רשתות אמיתיות.

**מה שזה לא היה: לא הרשמה, לא אישור אדמין, לא workaround לעקיפת IP.** רק
תיקון בדיקה — הנחתי שהחסימה גורפת ולא בדקתי אם יש נתיב HTTPS חלופי לאותה
מערכת בדיוק. השיעור: "מה לעשות כדי לפתוח חסימה" יכול להיות "לבדוק אם יש
דלת אחרת לגמרי", לא רק "לנסות לפתוח את הדלת החסומה".

## תוצאה ראשונה, אימות חי 27.08.2026 (`scripts/demo_dairy_gap.py`, סניף 144)

9 התאמות חד-משמעיות, ממוצע פער -2.5% (רוב הפריטים בדיוק במחיר המפוקח, 0.0%
פער). מגבלה מתועדת: גודל אריזה לשמנת (חמוצה/מתוקה) לא נאכף — אין שדה אריזה
במקור data.gov.il, ולא אומת מספיק כדי לקודד בביטחון — פריט "חצי מנה" (100 מ"ל
לעומת 200 המצופה) עדיין מתאים ומניב פער לא-מנורמל (-22.5%), מתועד כמגבלה ידועה
בקוד (`moag_matcher.py`), לא מוסתר.
